
## Sesión 13/02/2026 — cdv-back-snk-movements

### Lo que pedí

1. **Crear un template de documentación** a partir de la estructura del wiki existente (Writerside). El agente exploró la carpeta `wiki/` del proyecto, analizó los ficheros de configuración (`writerside.cfg`, `wiki.tree`, `v.list`) y los topics existentes, y generó `CLAUDE-WIKI-TEMPLATE.md` en la raíz del proyecto.

2. **Documentar los consumidores nuevos** que no estaban en el wiki. El agente comparó los 22 consumers reales del código con los 17 documentados en `business_logic.md` y detectó 5 sin documentar: `GeographicalBoundaryCountry`, `EmployeeInChargeOfProduct`, `ProductFormatImage`, `TariffSalesPrice` y `Conversion`. Para cada uno leyó el adapter, el mapper, el batch y el use case service, y añadió la sección correspondiente en `business_logic.md` y la entrada de módulo en `architecture.md`.

3. **Mejorar el template** para que sea suficientemente concreto y reproducible. Se añadieron: el patrón exacto de cada sección de consumer (descripción, colecciones del batch, tombstones), ejemplos reales de distintos niveles de complejidad, frases en español reutilizables y la regla de actualizar siempre los dos ficheros al añadir un consumer.

4. **Conectar el template con este repo** (`documentAI-blog/snk/templates/`). Queda establecido que cuando se pida sincronizar, el agente copiará el template con versión incremental (`snk-template_V0.0.X.md`).

### Conclusión

El flujo queda así: se desarrolla y refina el template en el proyecto snk-movements → cuando hay una versión estable, se publica aquí en `snk/templates/` con su número de versión.

